import { TaillepipePipe } from './taillepipe.pipe';

describe('TaillepipePipe', () => {
  it('create an instance', () => {
    const pipe = new TaillepipePipe();
    expect(pipe).toBeTruthy();
  });
});
